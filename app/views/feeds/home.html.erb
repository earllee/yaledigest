<!DOCTYPE html>
<html>
<body>
<div class="navbar navbar-fixed-top">
	<div class="navbar-inner navbar-inverse">
		<div class="container">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</a>

			<a class="brand" href="#">Yale Digest</a>

			<div class="nav-collapse collapse">
				<ul class="nav">
					<li class="active"><a href="/feeds/home">News</a></li>
					<li class=""><a href="/feeds/discover">Discover</a></li>
					<li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">
						Filters<b class="caret"></b></a>
					<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
						<li><a tabindex="-1" href="#">Stuff</a></li>
						<li><a tabindex="-1" href="#">Dropdown 2</a></li>
						<li><a tabindex="-1" href="#">Dropdown 3</a></li>
					</ul>

					</li>
				</ul>

			</div>
		</div>
	</div>	
</div>


<div id="board" class="container">
	<ol class="listing">
		<% i = 0 %>
		<% @articles.each do |article| %>
			<% src = case article["source"] 
					when "The Yale Herald" then "success"
					when "Yale Daily News" then "info"
					when "Yale News" then "important"
					else "default"
					end
			%>

			<li>
			<a href="#" data-toggle="collapse" data-target=	"#desc<%= i %>">
				<%= article["title"] %>
			</a>
			<span class="label label-<%= src %>"><%= article["source"] %></span>
			<p id="desc<%= i %>" class="collapse">
				<%= article["description"].html_safe %>
				<% readMoreLink = (article["content"].empty? ? article["link"] : "#content#{i}")%>
				<a href="<%= readMoreLink %>" data-toggle="modal">[Read more]</a>
			</p>
			<div id="content<%= i %>" class="modal hide fade">
				<div class="modal-header">
					<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
					<a href="<%= article["link"] %>"><%= article["title"] %></a> 
				</div>
				<div class="modal-body">
					<%= article["content"].html_safe %>
				</div>
			</div>
			</li>
			<% i += 1 %>
		<% end %>
	</ol>
	<hr>

</div>
</body>
